2026-06-29 | Deployment | Fix Vercel build: remove framework:vite from root vercel.json; use npm --prefix frontend so /src/main.tsx resolves.
2026-06-29 | Deployment | Server .env: APP_BASE_URL/CORS_ORIGINS -> fuel-app-two-rose.vercel.app; push docs; redeploy fuel-app on Vercel.
2026-06-29 | Deployment | Push master (0042e37); Vercel production live at https://fuelapp.mteinc.net with direct API bundle (fuelapp-api.mteinc.net); HANDOFF updated.
2026-06-24 | Deployment | Push: direct Vercel→tunnel API client, cross-origin session cookies, remove edge proxy; docs for CrazyDomains DNS.
2026-06-23 | Deployment | Fix API startup IndentationError in auth.py; fix .env CORS_ORIGINS typo (==).
2026-06-23 | Deployment | CrazyDomains + Vercel: direct API via VITE_API_BASE_URL (no Vercel /api rewrite); SESSION_COOKIE_SAMESITE=none for cross-origin auth; updated TUNNEL_SETUP.TXT and VERCEL_DEPLOY.TXT.
2026-06-17 | Deployment | Fix Vercel 502 DNS_HOSTNAME_EMPTY: remove edge API proxy (private LAN API_PROXY_URL); static vercel.json rewrites only; fix .vercelignore to exclude /api/ not frontend/api.
2026-06-11 | Deployment | Fix Vercel 404: root vercel.json for GitHub monorepo deploy, SPA rewrite /(.*) -> index.html.
2026-06-17 | API | Add GET / root response in production API-only mode (run_production); add deploy/check_tunnel_dns.bat.
2026-06-17 | Deployment | Add deploy/stop_api.bat; expand TUNNEL_SETUP.TXT with DNS and Vercel 502 troubleshooting.
2026-06-17 | Deployment | Fix Vercel GitHub build: remove invalid functions runtime from vercel.json; add .vercelignore for Python api/ package.
2026-06-16 | Deployment | Added TUNNEL_SETUP.TXT for exposing on-prem API to Vercel via Cloudflare Tunnel or port forward; updated VERCEL_DEPLOY.TXT checklist.
2026-06-16 | Deployment | Fix Vercel 404: root vercel.json for GitHub monorepo (frontend/dist output); remove broken SPA catch-all rewrite (Vite handles routing).
2026-06-11 | Deployment | Vercel frontend deploy: vercel.json, build-time API proxy script, VITE_API_BASE_URL support, VERCEL_DEPLOY.TXT.
2026-06-11 | Migration | Phase 4 production cutover: web.config serves frontend/dist and proxies /api/* to FastAPI :8000; run_production.bat and NSSM service run uvicorn; build_frontend.bat; LAN mode on :8000 with SPA from API; Streamlit moved to deploy/run_streamlit.bat (deprecated). Secure session cookies when APP_BASE_URL is HTTPS.
2026-06-11 | Migration | Phase 3 Purchasing, Billing, Reports, Dashboard: FastAPI services and routers for purchasing (fuel prices, PO gen, receiving), billing (actual qty, mark billed), reports (summary, CSV/PDF export), dashboard analytics, and document PDF endpoints; React pages wired in App.tsx with role guards, sidebar links, and home cards. Frontend build verified.
2026-04-30 | Migration | Phase 2 Requests + Approvals: requisitions API (submit, history, edit, prior-approved warning), approvals API (pending, approve/reject, quantity edit); React Requests and Approvals pages with mobile layout and sidebar badge.
2026-04-30 | Migration | Phase 1 Master Data: FastAPI vendors/vehicles CRUD API with role guards; React Master Data page (tabs, add/edit/deactivate). AppLayout sidebar added.
2026-04-30 | Migration | Phase 0 React + FastAPI scaffold: api/ (auth login/logout/me, session cookie), frontend/ (Vite React TS login + home), deploy/run_api.bat, deploy/run_frontend.bat, MIGRATION.TXT. Streamlit unchanged.
2026-04-30 | Deployment | IIS + HTTPS: web.config at project root, deploy/IIS_SETUP.txt, deploy/setup_iis.ps1; Streamlit config.toml updated for reverse-proxy (127.0.0.1:8501, headless).
2026-04-30 | Deployment | Added Windows Server production deployment: DEPLOYMENT.TXT, deploy/ scripts (install, production/LAN launchers, IIS web.config, NSSM service, firewall, DB backup). Fixed run_app.bat to use port 8501 with venv support.
2026-03-30 | Purchasing | Added an anytime "Update Fuel Prices" button and set session flag after price updates.
2026-03-30 | Purchasing | Left-aligned the anytime "Update Fuel Prices" button with the page section.
2026-03-30 | Purchasing | Fuel price update prompt is now a centered, blocking modal to be more attention-grabbing.
2026-02-16 | Purchaser access to Billing | Purchaser role now sees Billing in the sidebar and is permitted to open the Billing page.
2026-02-16 | PWA support (install to home screen) | Added pwa/manifest.webmanifest, pwa/sw.js, and pwa/icons/README. App injects manifest link and service worker registration when logged in. PWA_SETUP.txt documents serving manifest/sw/icons via reverse proxy and adding 192/512 icons.
2026-02-16 | Session persistence across refresh | Cookie-based session: user_sessions table (token_hash, user_id, expires_at). On login, create session and set fuel_session cookie via JS. On load without current_user, JS reads cookie and redirects to ?_session=TOKEN; app validates token and restores current_user. Logout clears cookie and deletes session. Refresh no longer logs user out.
2026-02-16 | Reports: date range filter | Added optional From date and To date filters (created_at). Inclusive range; both optional. CSV/PDF use same filtered data.
2026-02-16 | Reports: Vendor search from Master Data (autocomplete) | Vendor filter is now a type-to-filter text input plus a selectbox of vendors from Vendor Master Data; selection is exact match.
2026-02-16 | Reports: Vendor search and PDF download | Serial number search replaced with Vendor search (optional, partial match). Added Download report (PDF) button; report PDF shows table with Serial #, Plate, Qty, Unit, Total, Vendor, Status, PO Ref, Invoice # and grand total.
2026-02-16 | Reports: search and download | Reports page has optional search by Plate number and Serial number (partial, case-insensitive). Download report (CSV) button generates a CSV of the filtered results (Serial #, Plate, Quantity, Unit, Total Price, Vendor, Status, PO Ref, Invoice #, Requested By, Created).
2026-02-16 | Approvals: total price from latest vendor fuel price | Approvals page Total Price column now shows quantity × latest unit price (from vendor_fuel_prices by vendor and fuel type). FULLTANK or missing price shows —. list_pending_requisitions includes vendor_id and fuel_type.
2026-02-16 | Purchasing: vendor fuel prices and unit price from prices | New table vendor_fuel_prices (per-vendor diesel/unleaded/premium). When purchaser opens Purchasing, prompt \"Do you have fuel price updates?\" once per session; if Yes, dialog with one tab per vendor, each with Diesel/Unleaded/Premium fields and Update button. PO generation unit price field now defaults to current vendor fuel price by fuel type (editable).
2026-02-16 | Approvals Requested By: full name | list_pending_requisitions now returns requester full name (COALESCE full_name, username) so Approvals page Requested By column shows full name.
2026-02-16 | Approvals Vehicle column: Plate No - Make/Model | Vehicle column on Approvals (desktop and mobile) now shows "Plate No - Make/Model" using cars.model from pending requisition data.
2026-02-16 | PO printout Prepared By → Requested By | PO PDF signature line changed from "Prepared by" to "Requested by"; value now shows the requester's full name (same as remarks) instead of the PO preparer.
2026-02-16 | Approvals Mobile Layout (Auto) | Approvals module now supports a mobile-friendly card layout that is automatically selected based on browser width via approvals_layout URL parameter and a small JS detector. Desktop/table layout remains unchanged.
2026-02-16 | Billing Billed PO Summary PDF | Billing module now captures each batch of items marked as billed and offers a \"Billed PO Summary\" PDF download (invoice #, billing date, table of billed POs, grand total) via generate_billed_summary_pdf in pdf_gen.py.
2026-02-16 | Supervisor renamed to Approver | Role "supervisor" renamed to "approver" across app and DB. Migration in init_database updates existing users (supervisor → approver). Updated app.py (default page, sidebar, badges), database.py (hierarchy, migration), approvals.py, user_management.py, HANDOFF, SCRIPTS_OVERVIEW, README.
2026-02-16 | PO header company details spacing | Reduced spacing in PO printout header company details: line-height 0.4 (was 0.5) for address/contact/email, small negative top margins between lines.
2026-02-16 | PO Remarks use Users' Full Name | PO printout remarks line ("FOR ... WITH PLATE NO ...") now uses the requester's full name from the Users table (fetch_full_name_by_id) instead of the stored requestor_name on the requisition. build_po_pdf and safe_build_po_pdf now accept db_path; fallback to requester_name/requestor_name if user lookup fails.
2026-02-16 | Requestor from current user full name | Requestor field on New Request form (after Quantity) is read-only and pulled from the current user's full name. Stored in requisitions.requestor_name. Edit Request form shows Requestor as read-only (original requestor); save does not overwrite requestor_name.
2026-02-05 | SMTP configuration for Forgot Password | Added python-dotenv to load .env at app startup so SMTP and APP_BASE_URL can be set in a .env file. Created SMTP_SETUP.txt with step-by-step SMTP configuration (Option 1: .env file, Option 2: system env vars) and variable reference. Added .gitignore so .env (credentials) is not committed.
2026-02-05 | Forgot Password (Email-Based) | Added email-based Forgot Password flow. Login page has a right-aligned "Forgot password?" link. User enters email; if account exists, a one-time reset link is sent (SMTP via env: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_FROM, APP_BASE_URL). Reset link opens Set New Password form; token expires in 1 hour and is invalidated after use. Database: added email column to users, password_reset_tokens table, create_password_reset_token, find_valid_reset_token, invalidate_reset_token, fetch_user_by_email. User Management (Superuser): email field in add/edit user and user list. Added src/utils/email.py and env.example for SMTP/config.
2026-02-05 | Login Forgot Password Link | Replaced Forgot password button with a right-aligned link (text-align: right) on the login page. Link navigates to ?forgot=1 for the forgot-password flow.
2026-02-05 | Finance Renamed to Accounting | Renamed role "Finance" to "Accounting" across the app. Updated sidebar, all module role checks, database hierarchy and migration (admin/finance → accounting), init_admin.py (Accounting user and prompts). Existing DBs: migration updates legacy admin/finance roles to accounting.
2026-02-05 | Superuser Role & User Management | Added Superuser role with full access (all pages). Only Superuser can add, modify, or delete users; User Management page and actions restricted to Superuser. Finance and Supervisor no longer see User Management in sidebar. Role hierarchy: user < supervisor < purchaser < accounting < superuser.
2026-02-05 | Supervisor Permissions | Supervisor role can access Master Data and User Management (view/manage users) and approve requests. Sidebar and module checks updated for supervisor.
2026-02-05 | Billing FULLTANK Success Message | After updating actual quantity for FULLTANK in Billing, a short-lived success message is shown on the next page load via session state (billing_update_success), then cleared.
2026-02-05 | Purchasing PO Download UI | Replaced PO download icon with custom green download icon (assets/download_icon.png). Download button label set to "Download"; widened Download/Action columns in PO Generated table so "Download" fits on one line. Header and row download controls use icon + Download button.
2025-12-10 | PO Printout Header Layout | Restructured PO printout header layout to center company information (name, address, contact, email) on the page while keeping company logo on the left and Planters logo + ORIGINAL on the right. All three sections are vertically aligned at the same height using table-cell display with vertical-align: middle. Company info text is centered using text-align: center. Logo positions remain unchanged.
2025-12-10 | PO Printout Full Name & Date | Added full_name field to User Management (required). PO printout now displays full_name for Prepared By and Approved By fields instead of username. If full_name is empty, falls back to username. PO date now uses current date when generating PO (not requisition creation date). Changed unit display from "liters" to "LITERS" (uppercase) in PO printout. Added database migration to convert existing prepared_by/approved_by usernames to full_names. Updated create_user and update_user functions to require/support full_name. Added fetch_full_name_by_id helper function. Updated User Management UI to include full_name field in add/edit forms and display in user list.
2025-12-10 | Requests Company Logo Display | Added dynamic company logo display in top-right corner of Requests page when a vehicle is selected. Supports dual logos for MTEI (DDD_logo.png then mte_logo.png) and DIC (eskina_logo.png then dic_logo.png) displayed side-by-side at 120px each. Single logos for other companies: DSRDC → DSRDC_logo.png, all others/default → mte_logo.png. Logo appears only when a vehicle is selected and disappears when no vehicle is selected. Column layout adjusts automatically: [2.5, 1.5] for dual logos, [3, 1] for single logos. Updated get_company_logo_paths function to return list of logo paths for flexible display.
2025-12-10 | PO Printout Signature Fields | Added prepared_by and approved_by fields to Purchase Order printout. Database migration adds prepared_by and approved_by TEXT columns to requisitions table. When a requisition is approved, the approver's username is stored in approved_by. When a PO is generated, the preparer's username is stored in prepared_by. Updated list_requisitions_by_status to include these fields. PO PDF now displays actual usernames instead of empty strings. Added fetch_username_by_id helper function to database.py.
2025-12-10 | PO Printout Logo Handling | Enhanced logo handling for PO printout to gracefully handle missing planters_logo.png. System now tries alternative logo files (DSRDC_logo.png, DDD_logo.png, eskina_logo.png) if planters_logo.png is not found. Updated both render_purchase_order_html and build_po_pdf functions to implement fallback logo logic.
2025-12-10 | PO Printout Fuel Type | Added fuel type display to Purchase Order printout description field. Fuel type appears first line, followed by "***CONTAINER IS NOT ALLOWED***" on second line. Updated list_requisitions_by_status database query to include fuel_type field. Updated build_po_pdf function to pass fuel_type to PDF context.
2025-12-10 | Dashboard Price Trend | Added fuel price trend graph to Dashboard module showing monthly average prices for the last 12 months. Features: Line chart with separate lines for each fuel type (Diesel, Unleaded Gasoline, Premium Gasoline), optional vendor filter, price statistics summary (latest, min, max prices per fuel type), includes only confirmed prices (po_generated/received/billed statuses). Added get_fuel_price_trend database function to query monthly average prices grouped by fuel type.
2025-12-10 | Dashboard Module | Added new Dashboard page for Finance role with month/year selector. Features: Pie chart visualization of fuel usage (toggle between "By Vehicle" and "By Company"), Top 10 consuming vehicles per company section (shows ranked list for each company with total liters and request count), Separate FULLTANK requests list grouped by company. Dashboard uses plotly for chart rendering. Added database query functions: get_monthly_usage_by_vehicle, get_monthly_usage_by_company, get_top_vehicles_per_company, get_fulltank_requests_by_month. Added plotly to REQUIREMENTS.TXT.
2025-12-10 | Vehicle Company Field | Added required company field to vehicle add/edit forms in Master Data module. Dropdown options: MTEI, DSRDC, DIC, GCEIC, MTEI Trucking. Field is required (removed optional status). Added database migration to add company column to cars table. Updated upsert_car and update_car functions to support company parameter. Updated list_cars to include company.
2025-12-10 | Request Fuel Type Field | Added fuel_type field to request forms (add and edit) in Requests module. Fuel type is pulled from Vehicle Master Data. Locking logic: Diesel is locked (uneditable), Unleaded Gasoline can switch to Premium Gasoline (and vice versa), NULL fuel_type shows all 3 options. Fuel type is shown in confirmation dialog. Added fuel_type column to requisitions table and updated create_requisition/update_requisition functions. Updated list_cars to include fuel_type.
2025-12-10 | Vehicle Fuel Type Field | Added required fuel_type field to vehicle add/edit forms in Master Data module. Dropdown options: Diesel, Unleaded Gasoline, Premium Gasoline. Field is required (removed optional status). Added database migration to add fuel_type column to cars table. Updated upsert_car and update_car functions to support fuel_type parameter.
2025-12-10 | Full-Page Background Logo | Added temporary full-page background logo (mte_logo.png) with 20% transparency behind all modules. Can be disabled by setting ENABLE_BACKGROUND_LOGO = False in src/utils/styling.py. Logo is centered and non-intrusive.
2025-12-10 | Approvals Edit FULLTANK Conversion | Enhanced edit form in Approvals module to allow changing from FULLTANK to Numeric quantity mode. Added helpful message and improved quantity field handling when switching modes.
2025-12-10 | Approvals Button Icons | Replaced "Edit", "Approve", and "Reject" button text with icons (✏️, ✓, ✗) in Approvals module for cleaner, more compact UI.
2025-12-10 | Approvals Button Layout | Changed Edit, Approve, and Reject buttons in Approvals module to display side-by-side on one line using sub-columns within the action column for better space utilization.
2025-12-10 | Purchasing Button Text | Changed "Mark Received" button text to "Received" for shorter, cleaner label.
2025-12-10 | Purchasing Button Sizing | Made "Mark Received" button smaller to match download button height (38px) for consistent UI. Added CSS styling to ensure both buttons have the same height.
2025-12-10 | Purchasing Download Icon | Replaced "Download" text on download buttons and column header in Purchasing page with download icon (📥) for cleaner UI. Applied to both PO generation download buttons, receiving section download buttons, and the "Download" column header.
2025-12-10 | Temporary Styling System | Added temporary professional styling system with easy toggle. Created .streamlit/config.toml for base theme (darker blue primary color #0D47A1, darker gray backgrounds), src/utils/styling.py for custom CSS (enhanced forms, buttons, tables, status badges with darker color scheme), and ENABLE_CUSTOM_STYLING flag in app.py. Styling can be disabled by setting flag to False or deleting config.toml. Added STYLING_GUIDE.txt with instructions.
2025-12-10 | Reports Billed Sorting | Added sorting by Invoice Number for the "Billed" section on Reports page. Billed items are now sorted alphabetically by invoice number (case-insensitive), with entries without invoice numbers placed at the end. Other status groups remain unsorted (by creation date).
2025-12-10 | Confirmation Dialogs | Added st.dialog confirmation dialogs for critical actions: Submit Request (shows vehicle, vendor, quantity, notes), Generate PO (shows serial #, vehicle, vendor, quantity, pricing, PO reference), and Approve/Reject (shows request details). All confirmations require explicit "Yes, Confirm" click before proceeding, preventing accidental submissions.
2025-12-10 | Auto-Select Vendor from Vehicle | Updated Request page to automatically select vendor based on selected vehicle's default vendor from master data. When user selects a vehicle, the vendor dropdown auto-populates with that vehicle's default vendor. Vendor can still be manually changed if needed.
2025-12-10 | Request Quantity FULLTANK Option | Request page quantity now supports Numeric liters or FULLTANK via a toggle. FULLTANK sets unit to "FULLTANK" and hides numeric input. Displays "Full Tank" across Requests, Approvals, Purchasing, Billing, Reports, and PO PDF. Purchasing total is only computed for numeric quantities; FULLTANK shows no total.
2025-12-10 | Vendor Required & Unit Price Moved | Made vendor field required in request and edit forms (removed "(None)" option). Removed unit_price field from request page - purchasers now encode unit price when generating PO on Purchasing page. Updated update_requisition_po to accept and store unit_price, auto-calculating total_price. Request form validation now requires vendor selection.
2025-12-10 | Unit Field Removal | Removed unit selection field from request and edit forms. System now uses "liters" as the only unit of measurement. Updated all UI displays (Requests, Approvals, Purchasing, Billing, Reports) and PO PDF to show "liters" instead of dynamic unit field. Quantity field label updated to "Quantity (liters)" for clarity.
2025-12-10 | Request Page Redesign | Redesigned Fuel Requests page: entry form is now visible by default on page load, added "Request History" toggle button that shows/hides history section, history only loads when button is clicked (lazy loading for better performance), form clears after successful submission, removed "ADD ENTRY" button and Cancel button. Improves initial page load speed and provides form-first user experience.
2025-12-10 | User Management ADD USER | Added "ADD USER" button to User Management page. Entry form is hidden by default and only shows when "ADD USER" is clicked. Added Cancel button to form. Form auto-hides after successful submission. Matches the same pattern used in Master Data and Requests pages.
2025-12-10 | Sidebar Notification Badge | Added visual notification badge in sidebar showing pending approvals count for higher roles (Supervisor, Purchaser, Finance). Badge displays as "Approvals ● (count)" when there are pending items. Only visible to users with approval access.
2025-12-10 | Master Data UI Improvements | Changed "Cars" to "Vehicle" throughout Master Data page. Added column headers to Vehicle list (Plate Number, Make/Model, Default Vendor). Added "ADD ENTRY" buttons that toggle entry forms - forms are hidden by default and only show when "ADD ENTRY" is clicked. Added Cancel buttons to entry forms.
2025-12-10 | Master Data Edit Functionality | Added edit buttons and edit forms for Vendors and Cars in Master Data page. Users can now edit vendor names, addresses, car plate numbers, models, and vendor associations. Added update_vendor and update_car functions to database.py with validation to prevent duplicate names/plates.
2025-12-10 | Request Edit Functionality | Added edit button to Request page. Requests can be edited only once, strictly by creator or higher level users. Only pending requests can be edited. Added database fields is_edited and edited_by to track edit history. Implemented role hierarchy check (user < supervisor < purchaser < finance) for edit permissions.
2025-12-10 | PO Printout Design | Updated PO PDF template to match company design: added MTE and Planters Choice logos, restructured header with logo side-by-side with company info, added "Tell Nos.:" and "Email:" labels, right-aligned ORIGINAL/PO No./Date/Total with table, added serial number to PO header, aligned Prepared by/Approved by on same line.
2025-12-10 | Serial Number System | Added system-generated serial numbers (starting at 00001) to all requisitions, displayed serial numbers on all pages (Requests, Approvals, Purchasing, Billing, Reports), added serial number to PO printout header, and cleaned up entries without serial numbers.
2025-12-10 | Vendor Database | Changed vendor "contact" field to "address" throughout database schema, queries, and UI modules. Added migration function to handle existing data.
2025-12-09 | Login UI | Centered login form fields and aligned title/caption with form inputs. Restructured form to allow Enter key submission in Password field.
2025-12-09 | Approvals & Timestamps | Fixed approval permissions: only Supervisors and Finance can approve/reject requests (Purchasers can view but not approve). Fixed timestamp handling to use local time instead of UTC for all created_at and updated_at fields.
2025-12-09 | Form UX | Added keyboard navigation hints (Tab/Enter) and improved form field help text for better user experience in request form.
2025-12-09 | Duplicate Prevention | Added session state tracking and submission hash to prevent duplicate form submissions in request form.
2025-12-09 | Currency Symbol | Changed currency display from dollar sign ($) to peso sign (₱) throughout all UI modules and PDFs.
2025-12-09 | Pricing & Auto-Calc | Added unit_price and total_price columns to requisitions table, implemented auto-calc total in request form (Phase 4), updated all UI modules to display pricing, enhanced PO PDFs with pricing, and added cost summaries to reports.
2025-12-09 | User Management & Batch Billing | Added user management module for Finance role (create/edit/deactivate users), enhanced billing with checkbox-based batch selection per Phase 6 spec, and added user CRUD helpers to database.py.
2025-12-09 | Dependencies | Removed sqlite3 from requirements (stdlib) and ensured bcrypt is installed.
2025-12-09 | PO PDF Integration | Wired PO PDF generation into purchasing flow with download buttons and context handling.
2025-12-09 | PDF Gen | Added PO PDF generation utility (jinja2 + xhtml2pdf) and documentation updates.
2025-12-09 | Purchasing/Billing/Reports | Added PO/receiving/billing flows, requisition lifecycle helpers, reporting view, and docs updates.
2025-12-09 | Requests/Approvals | Added requisition CRUD helpers, requests UI with history, approvals UI with approve/reject, and role-filtered navigation updates.
2025-12-09 | Master Data CRUD | Implemented vendors/cars management UI with soft deletes, added DB helpers, and role-based navigation.
2025-12-09 | Phase 3 Shell | Added Streamlit login shell with role-based navigation, run_app.bat launcher, and DB helpers; updated docs.
2025-12-09 | Phase 2 (Auth Seed) | Implemented bcrypt hash/verify, added init_admin seeding script, and expanded script overview/docs.
2025-12-09 | Structure | Added package markers, auth placeholder, UI module placeholders, utils/pdf_gen placeholder, and data folder.
2025-12-09 | Phase 1 | Added core requirements, expanded database init with users/vendors/cars/requisitions, aligned DB filename, and updated docs.
2025-12-09 | Initialization | Bootstrapped project structure with database setup and documentation.
